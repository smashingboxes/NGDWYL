.main
  i.icon-logo
  p.description
    | Each morning you are credited $86,400 for the rest of your life.
    | What you don't spend is lost, it carries over no balance to the next day.
    | You must live in the present of today...

  .step1(ng-hide="showUser")
    h3.step1__title What would you do?

    .step__form
      form(ng-submit="processForm()")
        input(type="text", placeholder="TYPE", autofocus="true", ng-model="goal", ng-keyup="onKeyDown('goal')", ng-readonly="readonly")
        button.button.active(analytics-on="click", analytics-event="Click", analytics-category="Button", analytics-label="Next 1/2") 1/2 Next
        div
          span.error(ng-class="{'show': readonly, 'hidden': !readonly}") No more characters


  .step2(ng-show="showUser")
    h3.step2__title
      sup *
      | Enter Your Name or Twitter @Name

    .step__form
      form(ng-submit="processForm('next')")
        input(type="text", placeholder="@", autofocus="true", ng-model="user", ng-keyup="onKeyDown('user')")
        p.optional
          sub *
          | optional
        button.button(type="submit", analytics-on="click", analytics-event="Click", analytics-category="Button", analytics-label="Submit 1/2").active 2/2 Submit